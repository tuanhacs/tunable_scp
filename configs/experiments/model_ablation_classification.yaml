defaults: [../default.yaml]
task: classification
experiment:
  name: model_ablation_classification
  type: model_ablation
  empirical_trials: 500
  reference_trials: 500
  models: [logistic, gradient_boosting, random_forest, dnn]
datasets: [mnist]
method:
  delta: 1.0
budget:
  value: 4
